{
  "repo": "authority-network",
  "version": "4.6.0",
  "validation_tier": "V2 generated/content-heavy with provider-backed social boundary",
  "release_decision": "blocked only when hard_failures > 0",
  "profiles": [
    {
      "command": "npm run validate:changed",
      "severity": "REAL-RISK HARD FAILS; WARNINGS NONBLOCKING",
      "purpose": "Fast cache-aware checks without mutation."
    },
    {
      "command": "npm run validate:release",
      "severity": "REAL-RISK HARD FAILS; WARNINGS NONBLOCKING",
      "purpose": "One safe page-repair pass, cache-aware audit, deterministic proof, policy and journey checks."
    },
    {
      "command": "npm run validate:full",
      "severity": "REAL-RISK HARD FAILS; WARNINGS NONBLOCKING",
      "purpose": "Full uncached page proof and hostile cache fixtures."
    },
    {
      "command": "npm run validate:clean-rebuild",
      "severity": "HARD FAIL ON NONDETERMINISTIC GOVERNED OUTPUT",
      "purpose": "Two isolated deterministic derived builds must match."
    },
    {
      "command": "npm run validate:cache:self-test",
      "severity": "HARD FAIL ON UNSAFE CACHE BEHAVIOR",
      "purpose": "Proves cache miss, invalidation, failure non-reuse, and corruption handling."
    }
  ],
  "hard_fail_examples": [
    "malformed canonical JSON",
    "unsafe publication claim",
    "wrong-domain routing",
    "missing required evidence",
    "secret exposure",
    "broken social preflight",
    "nondeterministic governed output",
    "executable failure"
  ],
  "warning_examples": [
    "word-count target",
    "optional metadata",
    "cadence variation",
    "low cache hit rate",
    "nonblocking editorial preference"
  ],
  "not_applicable": [
    {
      "stage": "browser_visual_proof",
      "reason": "Static publication rendering remains a post-deploy manual/live proof."
    },
    {
      "stage": "authenticated_app_access",
      "reason": "No authenticated public application surface exists in this repository."
    }
  ],
  "v4_6_portfolio_citation_control": true
}
